
    const Katal =  KotUniL.eu.sirotin.kotunil.derived.Katal;   
    const kat = new Katal(1);
    
    const Qkat = new Katal(10**30).expression;
    const quettakat = Qkat;

    const Rkat = new Katal(10**27).expression;
    const ronnakat = Rkat;

    const Ykat = new Katal(10**24).expression;
    const yottakat = Ykat;

    const Zkat = new Katal(10**21).expression;
    const zettakat = Zkat;

    const Ekat = new Katal(10**18).expression;
    const exakat = Ekat;

    const Pkat = new Katal(10**15).expression;
    const petakat = Pkat;

    const Tkat = new Katal(10**12).expression;
    const terakat = Tkat;

    const Gkat = new Katal(10**9).expression;
    const gigakat = Gkat;

    const Mkat = new Katal(10**6).expression;
    const megakat = Mkat;

    const kkat = new Katal(10**3).expression;
    const kilokat = kkat;

    const hkat = new Katal(10**2).expression;
    const hectokat = hkat;

    const dakat = new Katal(10**1).expression;
    const decakat = dakat;

    const dkat = new Katal(10**-1).expression;
    const decikat = dkat;

    const ckat = new Katal(10**-2).expression;
    const centikat = ckat;

    const mkat = new Katal(10**-3).expression;
    const millikat = mkat;

    const μkat = new Katal(10**-6).expression;
    const microkat = μkat;

    const nkat = new Katal(10**-9).expression;
    const nanokat = nkat;

    const pkat = new Katal(10**-12).expression;
    const picokat = pkat;

    const fkat = new Katal(10**-15).expression;
    const femtokat = fkat;

    const akat = new Katal(10**-18).expression;
    const attokat = akat;

    const zkat = new Katal(10**-21).expression;
    const zeptokat = zkat;

    const ykat = new Katal(10**-24).expression;
    const yoctokat = ykat;

    const rkat = new Katal(10**-27).expression;
    const rontokat = rkat;

    const qkat = new Katal(10**-30).expression;
    const quectokat = qkat;