
    const Radian =  KotUniL.eu.sirotin.kotunil.derived.Radian;   
    const rad = new Radian(1);
    
    const Qrad = new Radian(10**30).expression;
    const quettarad = Qrad;

    const Rrad = new Radian(10**27).expression;
    const ronnarad = Rrad;

    const Yrad = new Radian(10**24).expression;
    const yottarad = Yrad;

    const Zrad = new Radian(10**21).expression;
    const zettarad = Zrad;

    const Erad = new Radian(10**18).expression;
    const exarad = Erad;

    const Prad = new Radian(10**15).expression;
    const petarad = Prad;

    const Trad = new Radian(10**12).expression;
    const terarad = Trad;

    const Grad = new Radian(10**9).expression;
    const gigarad = Grad;

    const Mrad = new Radian(10**6).expression;
    const megarad = Mrad;

    const krad = new Radian(10**3).expression;
    const kilorad = krad;

    const hrad = new Radian(10**2).expression;
    const hectorad = hrad;

    const darad = new Radian(10**1).expression;
    const decarad = darad;

    const drad = new Radian(10**-1).expression;
    const decirad = drad;

    const crad = new Radian(10**-2).expression;
    const centirad = crad;

    const mrad = new Radian(10**-3).expression;
    const millirad = mrad;

    const μrad = new Radian(10**-6).expression;
    const microrad = μrad;

    const nrad = new Radian(10**-9).expression;
    const nanorad = nrad;

    const prad = new Radian(10**-12).expression;
    const picorad = prad;

    const frad = new Radian(10**-15).expression;
    const femtorad = frad;

    const arad = new Radian(10**-18).expression;
    const attorad = arad;

    const zrad = new Radian(10**-21).expression;
    const zeptorad = zrad;

    const yrad = new Radian(10**-24).expression;
    const yoctorad = yrad;

    const rrad = new Radian(10**-27).expression;
    const rontorad = rrad;

    const qrad = new Radian(10**-30).expression;
    const quectorad = qrad;