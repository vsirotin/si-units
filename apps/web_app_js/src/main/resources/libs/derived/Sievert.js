
    const Sievert =  KotUniL.eu.sirotin.kotunil.derived.Sievert;   
    const Sv = new Sievert(1);
    
    const QSv = new Sievert(10**30);
    const quettaSv = QSv;

    const RSv = new Sievert(10**27);
    const ronnaSv = RSv;

    const YSv = new Sievert(10**24);
    const yottaSv = YSv;

    const ZSv = new Sievert(10**21);
    const zettaSv = ZSv;

    const ESv = new Sievert(10**18);
    const exaSv = ESv;

    const PSv = new Sievert(10**15);
    const petaSv = PSv;

    const TSv = new Sievert(10**12);
    const teraSv = TSv;

    const GSv = new Sievert(10**9);
    const gigaSv = GSv;

    const MSv = new Sievert(10**6);
    const megaSv = MSv;

    const kSv = new Sievert(10**3);
    const kiloSv = kSv;

    const hSv = new Sievert(10**2);
    const hectoSv = hSv;

    const daSv = new Sievert(10**1);
    const decaSv = daSv;

    const dSv = new Sievert(10**-1);
    const deciSv = dSv;

    const cSv = new Sievert(10**-2);
    const centiSv = cSv;

    const mSv = new Sievert(10**-3);
    const milliSv = mSv;

    const μSv = new Sievert(10**-6);
    const microSv = μSv;

    const nSv = new Sievert(10**-9);
    const nanoSv = nSv;

    const pSv = new Sievert(10**-12);
    const picoSv = pSv;

    const fSv = new Sievert(10**-15);
    const femtoSv = fSv;

    const aSv = new Sievert(10**-18);
    const attoSv = aSv;

    const zSv = new Sievert(10**-21);
    const zeptoSv = zSv;

    const ySv = new Sievert(10**-24);
    const yoctoSv = ySv;

    const rSv = new Sievert(10**-27);
    const rontoSv = rSv;

    const qSv = new Sievert(10**-30);
    const quectoSv = qSv;