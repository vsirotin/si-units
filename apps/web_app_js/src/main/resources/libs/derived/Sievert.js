
    const Sievert =  KotUniL.eu.sirotin.kotunil.derived.Sievert;   
    const Sv = new Sievert(1);
    
    const QSv = new Sievert(10**30).expression;
    const quettaSv = QSv;

    const RSv = new Sievert(10**27).expression;
    const ronnaSv = RSv;

    const YSv = new Sievert(10**24).expression;
    const yottaSv = YSv;

    const ZSv = new Sievert(10**21).expression;
    const zettaSv = ZSv;

    const ESv = new Sievert(10**18).expression;
    const exaSv = ESv;

    const PSv = new Sievert(10**15).expression;
    const petaSv = PSv;

    const TSv = new Sievert(10**12).expression;
    const teraSv = TSv;

    const GSv = new Sievert(10**9).expression;
    const gigaSv = GSv;

    const MSv = new Sievert(10**6).expression;
    const megaSv = MSv;

    const kSv = new Sievert(10**3).expression;
    const kiloSv = kSv;

    const hSv = new Sievert(10**2).expression;
    const hectoSv = hSv;

    const daSv = new Sievert(10**1).expression;
    const decaSv = daSv;

    const dSv = new Sievert(10**-1).expression;
    const deciSv = dSv;

    const cSv = new Sievert(10**-2).expression;
    const centiSv = cSv;

    const mSv = new Sievert(10**-3).expression;
    const milliSv = mSv;

    const μSv = new Sievert(10**-6).expression;
    const microSv = μSv;

    const nSv = new Sievert(10**-9).expression;
    const nanoSv = nSv;

    const pSv = new Sievert(10**-12).expression;
    const picoSv = pSv;

    const fSv = new Sievert(10**-15).expression;
    const femtoSv = fSv;

    const aSv = new Sievert(10**-18).expression;
    const attoSv = aSv;

    const zSv = new Sievert(10**-21).expression;
    const zeptoSv = zSv;

    const ySv = new Sievert(10**-24).expression;
    const yoctoSv = ySv;

    const rSv = new Sievert(10**-27).expression;
    const rontoSv = rSv;

    const qSv = new Sievert(10**-30).expression;
    const quectoSv = qSv;