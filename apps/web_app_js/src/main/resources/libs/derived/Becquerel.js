
    const Becquerel =  KotUniL.eu.sirotin.kotunil.derived.Becquerel;   
    const Bq = new Becquerel(1);
    
    const QBq = new Becquerel(10**30).expression;
    const quettaBq = QBq;

    const RBq = new Becquerel(10**27).expression;
    const ronnaBq = RBq;

    const YBq = new Becquerel(10**24).expression;
    const yottaBq = YBq;

    const ZBq = new Becquerel(10**21).expression;
    const zettaBq = ZBq;

    const EBq = new Becquerel(10**18).expression;
    const exaBq = EBq;

    const PBq = new Becquerel(10**15).expression;
    const petaBq = PBq;

    const TBq = new Becquerel(10**12).expression;
    const teraBq = TBq;

    const GBq = new Becquerel(10**9).expression;
    const gigaBq = GBq;

    const MBq = new Becquerel(10**6).expression;
    const megaBq = MBq;

    const kBq = new Becquerel(10**3).expression;
    const kiloBq = kBq;

    const hBq = new Becquerel(10**2).expression;
    const hectoBq = hBq;

    const daBq = new Becquerel(10**1).expression;
    const decaBq = daBq;

    const dBq = new Becquerel(10**-1).expression;
    const deciBq = dBq;

    const cBq = new Becquerel(10**-2).expression;
    const centiBq = cBq;

    const mBq = new Becquerel(10**-3).expression;
    const milliBq = mBq;

    const μBq = new Becquerel(10**-6).expression;
    const microBq = μBq;

    const nBq = new Becquerel(10**-9).expression;
    const nanoBq = nBq;

    const pBq = new Becquerel(10**-12).expression;
    const picoBq = pBq;

    const fBq = new Becquerel(10**-15).expression;
    const femtoBq = fBq;

    const aBq = new Becquerel(10**-18).expression;
    const attoBq = aBq;

    const zBq = new Becquerel(10**-21).expression;
    const zeptoBq = zBq;

    const yBq = new Becquerel(10**-24).expression;
    const yoctoBq = yBq;

    const rBq = new Becquerel(10**-27).expression;
    const rontoBq = rBq;

    const qBq = new Becquerel(10**-30).expression;
    const quectoBq = qBq;