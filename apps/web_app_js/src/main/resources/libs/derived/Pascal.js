
    const Pascal =  KotUniL.eu.sirotin.kotunil.derived.Pascal;   
    const Pa = new Pascal(1);
    
    const QPa = new Pascal(10**30).expression;
    const quettaPa = QPa;

    const RPa = new Pascal(10**27).expression;
    const ronnaPa = RPa;

    const YPa = new Pascal(10**24).expression;
    const yottaPa = YPa;

    const ZPa = new Pascal(10**21).expression;
    const zettaPa = ZPa;

    const EPa = new Pascal(10**18).expression;
    const exaPa = EPa;

    const PPa = new Pascal(10**15).expression;
    const petaPa = PPa;

    const TPa = new Pascal(10**12).expression;
    const teraPa = TPa;

    const GPa = new Pascal(10**9).expression;
    const gigaPa = GPa;

    const MPa = new Pascal(10**6).expression;
    const megaPa = MPa;

    const kPa = new Pascal(10**3).expression;
    const kiloPa = kPa;

    const hPa = new Pascal(10**2).expression;
    const hectoPa = hPa;

    const daPa = new Pascal(10**1).expression;
    const decaPa = daPa;

    const dPa = new Pascal(10**-1).expression;
    const deciPa = dPa;

    const cPa = new Pascal(10**-2).expression;
    const centiPa = cPa;

    const mPa = new Pascal(10**-3).expression;
    const milliPa = mPa;

    const μPa = new Pascal(10**-6).expression;
    const microPa = μPa;

    const nPa = new Pascal(10**-9).expression;
    const nanoPa = nPa;

    const pPa = new Pascal(10**-12).expression;
    const picoPa = pPa;

    const fPa = new Pascal(10**-15).expression;
    const femtoPa = fPa;

    const aPa = new Pascal(10**-18).expression;
    const attoPa = aPa;

    const zPa = new Pascal(10**-21).expression;
    const zeptoPa = zPa;

    const yPa = new Pascal(10**-24).expression;
    const yoctoPa = yPa;

    const rPa = new Pascal(10**-27).expression;
    const rontoPa = rPa;

    const qPa = new Pascal(10**-30).expression;
    const quectoPa = qPa;