
    const Volt =  KotUniL.eu.sirotin.kotunil.derived.Volt;   
    const V = new Volt(1);
    
    const QV = new Volt(10**30).expression;
    const quettaV = QV;

    const RV = new Volt(10**27).expression;
    const ronnaV = RV;

    const YV = new Volt(10**24).expression;
    const yottaV = YV;

    const ZV = new Volt(10**21).expression;
    const zettaV = ZV;

    const EV = new Volt(10**18).expression;
    const exaV = EV;

    const PV = new Volt(10**15).expression;
    const petaV = PV;

    const TV = new Volt(10**12).expression;
    const teraV = TV;

    const GV = new Volt(10**9).expression;
    const gigaV = GV;

    const MV = new Volt(10**6).expression;
    const megaV = MV;

    const kV = new Volt(10**3).expression;
    const kiloV = kV;

    const hV = new Volt(10**2).expression;
    const hectoV = hV;

    const daV = new Volt(10**1).expression;
    const decaV = daV;

    const dV = new Volt(10**-1).expression;
    const deciV = dV;

    const cV = new Volt(10**-2).expression;
    const centiV = cV;

    const mV = new Volt(10**-3).expression;
    const milliV = mV;

    const μV = new Volt(10**-6).expression;
    const microV = μV;

    const nV = new Volt(10**-9).expression;
    const nanoV = nV;

    const pV = new Volt(10**-12).expression;
    const picoV = pV;

    const fV = new Volt(10**-15).expression;
    const femtoV = fV;

    const aV = new Volt(10**-18).expression;
    const attoV = aV;

    const zV = new Volt(10**-21).expression;
    const zeptoV = zV;

    const yV = new Volt(10**-24).expression;
    const yoctoV = yV;

    const rV = new Volt(10**-27).expression;
    const rontoV = rV;

    const qV = new Volt(10**-30).expression;
    const quectoV = qV;