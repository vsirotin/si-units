
    const Steradian =  KotUniL.eu.sirotin.kotunil.derived.Steradian;   
    const sr = new Steradian(1);
    
    const Qsr = new Steradian(10**30).expression;
    const quettasr = Qsr;

    const Rsr = new Steradian(10**27).expression;
    const ronnasr = Rsr;

    const Ysr = new Steradian(10**24).expression;
    const yottasr = Ysr;

    const Zsr = new Steradian(10**21).expression;
    const zettasr = Zsr;

    const Esr = new Steradian(10**18).expression;
    const exasr = Esr;

    const Psr = new Steradian(10**15).expression;
    const petasr = Psr;

    const Tsr = new Steradian(10**12).expression;
    const terasr = Tsr;

    const Gsr = new Steradian(10**9).expression;
    const gigasr = Gsr;

    const Msr = new Steradian(10**6).expression;
    const megasr = Msr;

    const ksr = new Steradian(10**3).expression;
    const kilosr = ksr;

    const hsr = new Steradian(10**2).expression;
    const hectosr = hsr;

    const dasr = new Steradian(10**1).expression;
    const decasr = dasr;

    const dsr = new Steradian(10**-1).expression;
    const decisr = dsr;

    const csr = new Steradian(10**-2).expression;
    const centisr = csr;

    const msr = new Steradian(10**-3).expression;
    const millisr = msr;

    const μsr = new Steradian(10**-6).expression;
    const microsr = μsr;

    const nsr = new Steradian(10**-9).expression;
    const nanosr = nsr;

    const psr = new Steradian(10**-12).expression;
    const picosr = psr;

    const fsr = new Steradian(10**-15).expression;
    const femtosr = fsr;

    const asr = new Steradian(10**-18).expression;
    const attosr = asr;

    const zsr = new Steradian(10**-21).expression;
    const zeptosr = zsr;

    const ysr = new Steradian(10**-24).expression;
    const yoctosr = ysr;

    const rsr = new Steradian(10**-27).expression;
    const rontosr = rsr;

    const qsr = new Steradian(10**-30).expression;
    const quectosr = qsr;