
    const Hertz =  KotUniL.eu.sirotin.kotunil.derived.Hertz;   
    const Hz = new Hertz(1);
    
    const QHz = new Hertz(10**30).expression;
    const quettaHz = QHz;

    const RHz = new Hertz(10**27).expression;
    const ronnaHz = RHz;

    const YHz = new Hertz(10**24).expression;
    const yottaHz = YHz;

    const ZHz = new Hertz(10**21).expression;
    const zettaHz = ZHz;

    const EHz = new Hertz(10**18).expression;
    const exaHz = EHz;

    const PHz = new Hertz(10**15).expression;
    const petaHz = PHz;

    const THz = new Hertz(10**12).expression;
    const teraHz = THz;

    const GHz = new Hertz(10**9).expression;
    const gigaHz = GHz;

    const MHz = new Hertz(10**6).expression;
    const megaHz = MHz;

    const kHz = new Hertz(10**3).expression;
    const kiloHz = kHz;

    const hHz = new Hertz(10**2).expression;
    const hectoHz = hHz;

    const daHz = new Hertz(10**1).expression;
    const decaHz = daHz;

    const dHz = new Hertz(10**-1).expression;
    const deciHz = dHz;

    const cHz = new Hertz(10**-2).expression;
    const centiHz = cHz;

    const mHz = new Hertz(10**-3).expression;
    const milliHz = mHz;

    const μHz = new Hertz(10**-6).expression;
    const microHz = μHz;

    const nHz = new Hertz(10**-9).expression;
    const nanoHz = nHz;

    const pHz = new Hertz(10**-12).expression;
    const picoHz = pHz;

    const fHz = new Hertz(10**-15).expression;
    const femtoHz = fHz;

    const aHz = new Hertz(10**-18).expression;
    const attoHz = aHz;

    const zHz = new Hertz(10**-21).expression;
    const zeptoHz = zHz;

    const yHz = new Hertz(10**-24).expression;
    const yoctoHz = yHz;

    const rHz = new Hertz(10**-27).expression;
    const rontoHz = rHz;

    const qHz = new Hertz(10**-30).expression;
    const quectoHz = qHz;