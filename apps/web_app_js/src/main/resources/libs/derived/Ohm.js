
    const Ohm =  KotUniL.eu.sirotin.kotunil.derived.Ohm;   
    const Ω = new Ohm(1);
    
    const QΩ = new Ohm(10**30).expression;
    const quettaΩ = QΩ;

    const RΩ = new Ohm(10**27).expression;
    const ronnaΩ = RΩ;

    const YΩ = new Ohm(10**24).expression;
    const yottaΩ = YΩ;

    const ZΩ = new Ohm(10**21).expression;
    const zettaΩ = ZΩ;

    const EΩ = new Ohm(10**18).expression;
    const exaΩ = EΩ;

    const PΩ = new Ohm(10**15).expression;
    const petaΩ = PΩ;

    const TΩ = new Ohm(10**12).expression;
    const teraΩ = TΩ;

    const GΩ = new Ohm(10**9).expression;
    const gigaΩ = GΩ;

    const MΩ = new Ohm(10**6).expression;
    const megaΩ = MΩ;

    const kΩ = new Ohm(10**3).expression;
    const kiloΩ = kΩ;

    const hΩ = new Ohm(10**2).expression;
    const hectoΩ = hΩ;

    const daΩ = new Ohm(10**1).expression;
    const decaΩ = daΩ;

    const dΩ = new Ohm(10**-1).expression;
    const deciΩ = dΩ;

    const cΩ = new Ohm(10**-2).expression;
    const centiΩ = cΩ;

    const mΩ = new Ohm(10**-3).expression;
    const milliΩ = mΩ;

    const μΩ = new Ohm(10**-6).expression;
    const microΩ = μΩ;

    const nΩ = new Ohm(10**-9).expression;
    const nanoΩ = nΩ;

    const pΩ = new Ohm(10**-12).expression;
    const picoΩ = pΩ;

    const fΩ = new Ohm(10**-15).expression;
    const femtoΩ = fΩ;

    const aΩ = new Ohm(10**-18).expression;
    const attoΩ = aΩ;

    const zΩ = new Ohm(10**-21).expression;
    const zeptoΩ = zΩ;

    const yΩ = new Ohm(10**-24).expression;
    const yoctoΩ = yΩ;

    const rΩ = new Ohm(10**-27).expression;
    const rontoΩ = rΩ;

    const qΩ = new Ohm(10**-30).expression;
    const quectoΩ = qΩ;