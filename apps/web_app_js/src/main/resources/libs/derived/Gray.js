
    const Gray =  KotUniL.eu.sirotin.kotunil.derived.Gray;   
    const Gy = new Gray(1);
    
    const QGy = new Gray(10**30).expression;
    const quettaGy = QGy;

    const RGy = new Gray(10**27).expression;
    const ronnaGy = RGy;

    const YGy = new Gray(10**24).expression;
    const yottaGy = YGy;

    const ZGy = new Gray(10**21).expression;
    const zettaGy = ZGy;

    const EGy = new Gray(10**18).expression;
    const exaGy = EGy;

    const PGy = new Gray(10**15).expression;
    const petaGy = PGy;

    const TGy = new Gray(10**12).expression;
    const teraGy = TGy;

    const GGy = new Gray(10**9).expression;
    const gigaGy = GGy;

    const MGy = new Gray(10**6).expression;
    const megaGy = MGy;

    const kGy = new Gray(10**3).expression;
    const kiloGy = kGy;

    const hGy = new Gray(10**2).expression;
    const hectoGy = hGy;

    const daGy = new Gray(10**1).expression;
    const decaGy = daGy;

    const dGy = new Gray(10**-1).expression;
    const deciGy = dGy;

    const cGy = new Gray(10**-2).expression;
    const centiGy = cGy;

    const mGy = new Gray(10**-3).expression;
    const milliGy = mGy;

    const μGy = new Gray(10**-6).expression;
    const microGy = μGy;

    const nGy = new Gray(10**-9).expression;
    const nanoGy = nGy;

    const pGy = new Gray(10**-12).expression;
    const picoGy = pGy;

    const fGy = new Gray(10**-15).expression;
    const femtoGy = fGy;

    const aGy = new Gray(10**-18).expression;
    const attoGy = aGy;

    const zGy = new Gray(10**-21).expression;
    const zeptoGy = zGy;

    const yGy = new Gray(10**-24).expression;
    const yoctoGy = yGy;

    const rGy = new Gray(10**-27).expression;
    const rontoGy = rGy;

    const qGy = new Gray(10**-30).expression;
    const quectoGy = qGy;