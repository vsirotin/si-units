
    const Gray =  KotUniL.eu.sirotin.kotunil.derived.Gray;   
    const Gy = new Gray(1);
    
    const QGy = new Gray(10**30);
    const quettaGy = QGy;

    const RGy = new Gray(10**27);
    const ronnaGy = RGy;

    const YGy = new Gray(10**24);
    const yottaGy = YGy;

    const ZGy = new Gray(10**21);
    const zettaGy = ZGy;

    const EGy = new Gray(10**18);
    const exaGy = EGy;

    const PGy = new Gray(10**15);
    const petaGy = PGy;

    const TGy = new Gray(10**12);
    const teraGy = TGy;

    const GGy = new Gray(10**9);
    const gigaGy = GGy;

    const MGy = new Gray(10**6);
    const megaGy = MGy;

    const kGy = new Gray(10**3);
    const kiloGy = kGy;

    const hGy = new Gray(10**2);
    const hectoGy = hGy;

    const daGy = new Gray(10**1);
    const decaGy = daGy;

    const dGy = new Gray(10**-1);
    const deciGy = dGy;

    const cGy = new Gray(10**-2);
    const centiGy = cGy;

    const mGy = new Gray(10**-3);
    const milliGy = mGy;

    const μGy = new Gray(10**-6);
    const microGy = μGy;

    const nGy = new Gray(10**-9);
    const nanoGy = nGy;

    const pGy = new Gray(10**-12);
    const picoGy = pGy;

    const fGy = new Gray(10**-15);
    const femtoGy = fGy;

    const aGy = new Gray(10**-18);
    const attoGy = aGy;

    const zGy = new Gray(10**-21);
    const zeptoGy = zGy;

    const yGy = new Gray(10**-24);
    const yoctoGy = yGy;

    const rGy = new Gray(10**-27);
    const rontoGy = rGy;

    const qGy = new Gray(10**-30);
    const quectoGy = qGy;