        
function testLumen() {
   console.log("-Start testLumen");      
    
    checkObjects(Qlm, new Lumen(10**30));
    checkObjects(quettalm, Qlm);

    checkObjects(Rlm, new Lumen(10**27));
    checkObjects(ronnalm, Rlm);

    checkObjects(Ylm, new Lumen(10**24));
    checkObjects(yottalm, Ylm);

    checkObjects(Zlm, new Lumen(10**21));
    checkObjects(zettalm, Zlm);

    checkObjects(Elm, new Lumen(10**18));
    checkObjects(exalm, Elm);

    checkObjects(Plm, new Lumen(10**15));
    checkObjects(petalm, Plm);

    checkObjects(Tlm, new Lumen(10**12));
    checkObjects(teralm, Tlm);

    checkObjects(Glm, new Lumen(10**9));
    checkObjects(gigalm, Glm);

    checkObjects(Mlm, new Lumen(10**6));
    checkObjects(megalm, Mlm);

    checkObjects(klm, new Lumen(10**3));
    checkObjects(kilolm, klm);

    checkObjects(hlm, new Lumen(10**2));
    checkObjects(hectolm, hlm);

    checkObjects(dalm, new Lumen(10**1));
    checkObjects(decalm, dalm);

    checkObjects(dlm, new Lumen(10**-1));
    checkObjects(decilm, dlm);

    checkObjects(clm, new Lumen(10**-2));
    checkObjects(centilm, clm);

    checkObjects(mlm, new Lumen(10**-3));
    checkObjects(millilm, mlm);

    checkObjects(μlm, new Lumen(10**-6));
    checkObjects(microlm, μlm);

    checkObjects(nlm, new Lumen(10**-9));
    checkObjects(nanolm, nlm);

    checkObjects(plm, new Lumen(10**-12));
    checkObjects(picolm, plm);

    checkObjects(flm, new Lumen(10**-15));
    checkObjects(femtolm, flm);

    checkObjects(alm, new Lumen(10**-18));
    checkObjects(attolm, alm);

    checkObjects(zlm, new Lumen(10**-21));
    checkObjects(zeptolm, zlm);

    checkObjects(ylm, new Lumen(10**-24));
    checkObjects(yoctolm, ylm);

    checkObjects(rlm, new Lumen(10**-27));
    checkObjects(rontolm, rlm);

    checkObjects(qlm, new Lumen(10**-30));
    checkObjects(quectolm, qlm);
    console.log("-Fin testLumen");
}
