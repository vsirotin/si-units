
    const Ohm =  KotUniL.eu.sirotin.kotunil.derived.Ohm;   
    const Ω = new Ohm(1);
    
    const QΩ = new Ohm(10**30);
    const quettaΩ = QΩ;

    const RΩ = new Ohm(10**27);
    const ronnaΩ = RΩ;

    const YΩ = new Ohm(10**24);
    const yottaΩ = YΩ;

    const ZΩ = new Ohm(10**21);
    const zettaΩ = ZΩ;

    const EΩ = new Ohm(10**18);
    const exaΩ = EΩ;

    const PΩ = new Ohm(10**15);
    const petaΩ = PΩ;

    const TΩ = new Ohm(10**12);
    const teraΩ = TΩ;

    const GΩ = new Ohm(10**9);
    const gigaΩ = GΩ;

    const MΩ = new Ohm(10**6);
    const megaΩ = MΩ;

    const kΩ = new Ohm(10**3);
    const kiloΩ = kΩ;

    const hΩ = new Ohm(10**2);
    const hectoΩ = hΩ;

    const daΩ = new Ohm(10**1);
    const decaΩ = daΩ;

    const dΩ = new Ohm(10**-1);
    const deciΩ = dΩ;

    const cΩ = new Ohm(10**-2);
    const centiΩ = cΩ;

    const mΩ = new Ohm(10**-3);
    const milliΩ = mΩ;

    const μΩ = new Ohm(10**-6);
    const microΩ = μΩ;

    const nΩ = new Ohm(10**-9);
    const nanoΩ = nΩ;

    const pΩ = new Ohm(10**-12);
    const picoΩ = pΩ;

    const fΩ = new Ohm(10**-15);
    const femtoΩ = fΩ;

    const aΩ = new Ohm(10**-18);
    const attoΩ = aΩ;

    const zΩ = new Ohm(10**-21);
    const zeptoΩ = zΩ;

    const yΩ = new Ohm(10**-24);
    const yoctoΩ = yΩ;

    const rΩ = new Ohm(10**-27);
    const rontoΩ = rΩ;

    const qΩ = new Ohm(10**-30);
    const quectoΩ = qΩ;