
        const Mole = KotUniL.eu.sirotin.kotunil.base.Mole;
        const mol = new Mole(1); 
        
        const Qmol = new Mole(10**30);
        const quettamole = Qmol;

        const Rmol = new Mole(10**27);
        const ronnamole = Rmol;

        const Ymol = new Mole(10**24);
        const yottamole = Ymol;

        const Zmol = new Mole(10**21);
        const zettamole = Zmol;

        const Emol = new Mole(10**18);
        const examole = Emol;

        const Pmol = new Mole(10**15);
        const petamole = Pmol;

        const Tmol = new Mole(10**12);
        const teramole = Tmol;

        const Gmol = new Mole(10**9);
        const gigamole = Gmol;

        const Mmol = new Mole(10**6);
        const megamole = Mmol;

        const kmol = new Mole(10**3);
        const kilomole = kmol;

        const hmol = new Mole(10**2);
        const hectomole = hmol;

        const damol = new Mole(10**1);
        const decamole = damol;

        const dmol = new Mole(10**-1);
        const decimole = dmol;

        const cmol = new Mole(10**-2);
        const centimole = cmol;

        const mmol = new Mole(10**-3);
        const millimole = mmol;

        const μmol = new Mole(10**-6);
        const micromole = μmol;

        const nmol = new Mole(10**-9);
        const nanomole = nmol;

        const pmol = new Mole(10**-12);
        const picomole = pmol;

        const fmol = new Mole(10**-15);
        const femtomole = fmol;

        const amol = new Mole(10**-18);
        const attomole = amol;

        const zmol = new Mole(10**-21);
        const zeptomole = zmol;

        const ymol = new Mole(10**-24);
        const yoctomole = ymol;

        const rmol = new Mole(10**-27);
        const rontomole = rmol;

        const qmol = new Mole(10**-30);
        const quectomole = qmol;