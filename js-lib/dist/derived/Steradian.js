
    const Steradian =  KotUniL.eu.sirotin.kotunil.derived.Steradian;   
    const sr = new Steradian(1);
    
    const Qsr = new Steradian(10**30);
    const quettasr = Qsr;

    const Rsr = new Steradian(10**27);
    const ronnasr = Rsr;

    const Ysr = new Steradian(10**24);
    const yottasr = Ysr;

    const Zsr = new Steradian(10**21);
    const zettasr = Zsr;

    const Esr = new Steradian(10**18);
    const exasr = Esr;

    const Psr = new Steradian(10**15);
    const petasr = Psr;

    const Tsr = new Steradian(10**12);
    const terasr = Tsr;

    const Gsr = new Steradian(10**9);
    const gigasr = Gsr;

    const Msr = new Steradian(10**6);
    const megasr = Msr;

    const ksr = new Steradian(10**3);
    const kilosr = ksr;

    const hsr = new Steradian(10**2);
    const hectosr = hsr;

    const dasr = new Steradian(10**1);
    const decasr = dasr;

    const dsr = new Steradian(10**-1);
    const decisr = dsr;

    const csr = new Steradian(10**-2);
    const centisr = csr;

    const msr = new Steradian(10**-3);
    const millisr = msr;

    const μsr = new Steradian(10**-6);
    const microsr = μsr;

    const nsr = new Steradian(10**-9);
    const nanosr = nsr;

    const psr = new Steradian(10**-12);
    const picosr = psr;

    const fsr = new Steradian(10**-15);
    const femtosr = fsr;

    const asr = new Steradian(10**-18);
    const attosr = asr;

    const zsr = new Steradian(10**-21);
    const zeptosr = zsr;

    const ysr = new Steradian(10**-24);
    const yoctosr = ysr;

    const rsr = new Steradian(10**-27);
    const rontosr = rsr;

    const qsr = new Steradian(10**-30);
    const quectosr = qsr;