
    const Hertz =  KotUniL.eu.sirotin.kotunil.derived.Hertz;   
    const Hz = new Hertz(1);
    
    const QHz = new Hertz(10**30);
    const quettaHz = QHz;

    const RHz = new Hertz(10**27);
    const ronnaHz = RHz;

    const YHz = new Hertz(10**24);
    const yottaHz = YHz;

    const ZHz = new Hertz(10**21);
    const zettaHz = ZHz;

    const EHz = new Hertz(10**18);
    const exaHz = EHz;

    const PHz = new Hertz(10**15);
    const petaHz = PHz;

    const THz = new Hertz(10**12);
    const teraHz = THz;

    const GHz = new Hertz(10**9);
    const gigaHz = GHz;

    const MHz = new Hertz(10**6);
    const megaHz = MHz;

    const kHz = new Hertz(10**3);
    const kiloHz = kHz;

    const hHz = new Hertz(10**2);
    const hectoHz = hHz;

    const daHz = new Hertz(10**1);
    const decaHz = daHz;

    const dHz = new Hertz(10**-1);
    const deciHz = dHz;

    const cHz = new Hertz(10**-2);
    const centiHz = cHz;

    const mHz = new Hertz(10**-3);
    const milliHz = mHz;

    const μHz = new Hertz(10**-6);
    const microHz = μHz;

    const nHz = new Hertz(10**-9);
    const nanoHz = nHz;

    const pHz = new Hertz(10**-12);
    const picoHz = pHz;

    const fHz = new Hertz(10**-15);
    const femtoHz = fHz;

    const aHz = new Hertz(10**-18);
    const attoHz = aHz;

    const zHz = new Hertz(10**-21);
    const zeptoHz = zHz;

    const yHz = new Hertz(10**-24);
    const yoctoHz = yHz;

    const rHz = new Hertz(10**-27);
    const rontoHz = rHz;

    const qHz = new Hertz(10**-30);
    const quectoHz = qHz;